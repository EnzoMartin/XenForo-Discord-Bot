const db = require('../modules/database');
const Log = require('../modules/logging');
const async = require('async');
const createDiscordIdField = require('../queries/createDiscordIdField.sql');

//TODO: Add the custom field to the DB?
module.exports = {
    createCustomFields: function(callback){

    }
};